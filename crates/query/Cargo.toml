[package]
name = "kode-query"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
kode-storage = { path = "../storage" }
kode-graph = { path = "../graph" }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
